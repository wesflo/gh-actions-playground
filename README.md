# gh-actions-playground

Hello World npm module with:
- Lit web component (`<gh-hello>`)
- Utils + unit tests (Vitest + happy-dom)
- Vite library build (ESM + CJS)
- Separate IIFE build

## Scripts

- `npm test`
- `npm run build`
- `npm run build:iife`
